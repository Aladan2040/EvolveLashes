---
// Navbar.astro - Premium animated navigation
import logoImg from "../assets/logo-evolve.svg";

const currentPath = Astro.url.pathname;
const isHome = currentPath === "/" || currentPath === "";
---

<nav
    id="navbar"
    class={`fixed top-0 left-0 w-full z-50 transition-all duration-500 ${isHome ? "bg-transparent py-4 md:py-6" : "bg-white/90 backdrop-blur-xl shadow-lg shadow-black/5 py-2"}`}
    data-is-home={isHome}
    style="animation: navSlideDown 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;"
>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16 md:h-20">
            <!-- LOGO -->
            <a
                href="/"
                class="flex-shrink-0 cursor-pointer flex items-center group"
            >
                <img
                    id="logo-img"
                    class={`transition-all duration-500 h-auto object-contain ${isHome ? "w-32 md:w-48 filter brightness-0 invert" : "w-28 md:w-40"}`}
                    src={logoImg.src}
                    alt="Evolve Lash Logo"
                />
            </a>

            <!-- MENÚ DESKTOP -->
            <div class="hidden md:flex space-x-6 lg:space-x-8 items-center">
                <a
                    href="/"
                    id="nav-link-inicio"
                    class={`relative font-medium transition-all duration-300 hover:text-evolve-accent text-base lg:text-lg group ${isHome ? "text-white" : "text-gray-600"}`}
                >
                    Inicio
                    <span
                        class={`absolute -bottom-1 left-0 h-0.5 bg-evolve-accent transition-all duration-300 rounded-full ${currentPath === "/" ? "w-full" : "w-0 group-hover:w-full"}`}
                    ></span>
                </a>
                <a
                    href="/services"
                    id="nav-link-servicios"
                    class={`relative font-medium transition-all duration-300 hover:text-evolve-accent text-base lg:text-lg group ${isHome ? "text-white" : "text-gray-600"}`}
                >
                    Servicios
                    <span
                        class={`absolute -bottom-1 left-0 h-0.5 bg-evolve-accent transition-all duration-300 rounded-full ${currentPath === "/services" ? "w-full" : "w-0 group-hover:w-full"}`}
                    ></span>
                </a>
                <button
                    id="btn-agendar-desktop"
                    onclick="window.open('https://wa.me/56975598631?text=Hola%20EvolveLashes,%20quiero%20una%20cita.', '_blank')"
                    class="relative overflow-hidden px-5 lg:px-7 py-2.5 rounded-full font-semibold transition-all duration-300 cursor-pointer bg-evolve-accent text-white hover:bg-evolve-dark shadow-[0_4px_15px_rgba(197,160,101,0.4)] hover:shadow-[0_4px_25px_rgba(34,34,34,0.4)] hover:scale-105 active:scale-95 shimmer-hover"
                >
                    Agendar Cita
                </button>
            </div>

            <!-- BOTÓN MENÚ MÓVIL (hamburguesa animada) -->
            <div class="md:hidden flex items-center">
                <button
                    id="mobile-menu-btn"
                    class={`relative w-10 h-10 flex items-center justify-center cursor-pointer focus:outline-none ${isHome ? "text-white" : "text-evolve-dark"}`}
                    aria-label="Menú"
                >
                    <span class="hamburger-line hamburger-top"></span>
                    <span class="hamburger-line hamburger-middle"></span>
                    <span class="hamburger-line hamburger-bottom"></span>
                </button>
            </div>
        </div>
    </div>

    <!-- MENÚ MÓVIL FULLSCREEN OVERLAY -->
    <div
        id="mobile-menu"
        class="md:hidden fixed inset-0 z-[100] pointer-events-none"
    >
        <!-- Background overlay -->
        <div
            id="mobile-menu-bg"
            class="absolute inset-0 bg-evolve-dark/95 backdrop-blur-xl opacity-0 transition-opacity duration-500"
        >
        </div>

        <!-- Content -->
        <div
            id="mobile-menu-content"
            class="relative z-10 flex flex-col items-center justify-center h-full px-8"
        >
            <nav class="flex flex-col items-center gap-8">
                <a
                    href="/"
                    class="mobile-nav-link text-2xl font-semibold text-white hover:text-evolve-accent transition-all duration-300 opacity-0 translate-y-6"
                    style="transition-delay: 0.1s;"
                >
                    Inicio
                </a>
                <a
                    href="/services"
                    class="mobile-nav-link text-2xl font-semibold text-white hover:text-evolve-accent transition-all duration-300 opacity-0 translate-y-6"
                    style="transition-delay: 0.2s;"
                >
                    Servicios
                </a>
                <div
                    class="w-16 h-px bg-white/20 my-2 mobile-nav-link opacity-0 translate-y-6"
                    style="transition-delay: 0.3s;"
                >
                </div>
                <button
                    onclick="window.open('https://wa.me/56975598631?text=Hola%20EvolveLashes,%20quiero%20una%20cita.', '_blank')"
                    class="mobile-nav-link bg-evolve-accent text-white px-10 py-4 rounded-full font-bold shadow-lg text-lg cursor-pointer hover:bg-white hover:text-evolve-dark transition-all duration-300 flex items-center gap-3 opacity-0 translate-y-6"
                    style="transition-delay: 0.35s;"
                >
                    <svg
                        class="w-5 h-5"
                        fill="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"
                        ></path>
                    </svg>
                    Agendar por WhatsApp
                </button>
            </nav>
        </div>
    </div>
</nav>

<!-- Spacer for fixed navbar on non-home pages -->
{!isHome && <div class="h-20 md:h-24" />}

<style>
    /* Nav slide-down entrance */
    @keyframes navSlideDown {
        from {
            opacity: 0;
            transform: translateY(-100%);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Hamburger animated lines */
    .hamburger-line {
        position: absolute;
        width: 22px;
        height: 2px;
        background: currentColor;
        border-radius: 2px;
        transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .hamburger-top {
        transform: translateY(-7px);
    }
    .hamburger-middle {
        transform: translateY(0);
    }
    .hamburger-bottom {
        transform: translateY(7px);
    }

    /* Active state → X */
    .menu-open .hamburger-top {
        transform: rotate(45deg) translateY(0);
    }
    .menu-open .hamburger-middle {
        opacity: 0;
        transform: scaleX(0);
    }
    .menu-open .hamburger-bottom {
        transform: rotate(-45deg) translateY(0);
    }

    /* Mobile menu open state */
    .mobile-menu-active {
        pointer-events: auto !important;
    }

    .mobile-menu-active #mobile-menu-bg {
        opacity: 1 !important;
    }

    .mobile-menu-active .mobile-nav-link {
        opacity: 1 !important;
        transform: translateY(0) !important;
    }
</style>

<script>
    document.addEventListener("DOMContentLoaded", () => {
        const navbar = document.getElementById("navbar");
        const logoImg = document.getElementById("logo-img");
        const mobileMenuBtn = document.getElementById("mobile-menu-btn");
        const mobileMenu = document.getElementById("mobile-menu");
        const navLinkInicio = document.getElementById("nav-link-inicio");
        const navLinkServicios = document.getElementById("nav-link-servicios");

        const isHome = navbar?.dataset.isHome === "true";
        let isMenuOpen = false;

        // Toggle menú móvil fullscreen
        mobileMenuBtn?.addEventListener("click", () => {
            isMenuOpen = !isMenuOpen;

            if (isMenuOpen) {
                mobileMenu?.classList.add("mobile-menu-active");
                mobileMenuBtn?.classList.add("menu-open");
                mobileMenuBtn?.classList.remove("text-white");
                mobileMenuBtn?.classList.add("text-white");
                document.body.style.overflow = "hidden";
            } else {
                mobileMenu?.classList.remove("mobile-menu-active");
                mobileMenuBtn?.classList.remove("menu-open");
                document.body.style.overflow = "";
            }
        });

        // Cerrar menú al hacer click en un enlace
        mobileMenu?.querySelectorAll("a, button").forEach((link) => {
            link.addEventListener("click", () => {
                isMenuOpen = false;
                mobileMenu?.classList.remove("mobile-menu-active");
                mobileMenuBtn?.classList.remove("menu-open");
                document.body.style.overflow = "";
            });
        });

        // Manejar scroll
        if (isHome) {
            let lastScroll = 0;
            const handleScroll = () => {
                const scrolled = window.scrollY > 50;

                if (scrolled) {
                    navbar?.classList.remove(
                        "bg-transparent",
                        "py-4",
                        "md:py-6",
                    );
                    navbar?.classList.add(
                        "bg-white/90",
                        "backdrop-blur-xl",
                        "shadow-lg",
                        "shadow-black/5",
                        "py-2",
                    );

                    logoImg?.classList.remove(
                        "w-32",
                        "md:w-48",
                        "filter",
                        "brightness-0",
                        "invert",
                    );
                    logoImg?.classList.add("w-28", "md:w-40");

                    navLinkInicio?.classList.remove("text-white");
                    navLinkInicio?.classList.add("text-gray-600");
                    navLinkServicios?.classList.remove("text-white");
                    navLinkServicios?.classList.add("text-gray-600");

                    mobileMenuBtn?.classList.remove("text-white");
                    mobileMenuBtn?.classList.add("text-evolve-dark");
                } else {
                    navbar?.classList.add("bg-transparent", "py-4", "md:py-6");
                    navbar?.classList.remove(
                        "bg-white/90",
                        "backdrop-blur-xl",
                        "shadow-lg",
                        "shadow-black/5",
                        "py-2",
                    );

                    logoImg?.classList.add(
                        "w-32",
                        "md:w-48",
                        "filter",
                        "brightness-0",
                        "invert",
                    );
                    logoImg?.classList.remove("w-28", "md:w-40");

                    navLinkInicio?.classList.add("text-white");
                    navLinkInicio?.classList.remove("text-gray-600");
                    navLinkServicios?.classList.add("text-white");
                    navLinkServicios?.classList.remove("text-gray-600");

                    mobileMenuBtn?.classList.add("text-white");
                    mobileMenuBtn?.classList.remove("text-evolve-dark");
                }

                lastScroll = window.scrollY;
            };

            window.addEventListener("scroll", handleScroll, { passive: true });
            handleScroll();
        }
    });
</script>
