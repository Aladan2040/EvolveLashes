---
// Hero.astro - Hero con animaciones modernas sin video
---

<section class="hero-section relative h-screen flex items-center justify-center overflow-hidden">
    <!-- Fondo animado con gradientes -->
    <div class="absolute inset-0 bg-gradient-to-br from-[#1a1a1a] via-[#2d2d2d] to-[#1a1a1a]">
        <!-- Partículas/elementos decorativos animados -->
        <div class="absolute inset-0 overflow-hidden">
            <!-- Círculos flotantes con blur -->
            <div class="floating-circle circle-1"></div>
            <div class="floating-circle circle-2"></div>
            <div class="floating-circle circle-3"></div>
            <div class="floating-circle circle-4"></div>
            <div class="floating-circle circle-5"></div>

            <!-- Líneas decorativas animadas -->
            <div class="animated-line line-1"></div>
            <div class="animated-line line-2"></div>
            <div class="animated-line line-3"></div>

            <!-- Destellos dorados -->
            <div class="sparkle sparkle-1"></div>
            <div class="sparkle sparkle-2"></div>
            <div class="sparkle sparkle-3"></div>
            <div class="sparkle sparkle-4"></div>
            <div class="sparkle sparkle-5"></div>
            <div class="sparkle sparkle-6"></div>
        </div>

        <!-- Patrón decorativo sutil -->
        <div class="absolute inset-0 lash-pattern"></div>
    </div>

    <!-- Overlay con gradiente -->
    <div class="absolute inset-0 bg-gradient-to-b from-black/40 via-transparent to-black/60"></div>

    <!-- Contenido del Hero -->
    <div class="relative z-10 text-center px-4 sm:px-6 max-w-4xl hero-content">
        <!-- Badge VIP -->
        <div class="badge-container inline-flex items-center gap-2 py-1.5 md:py-2 px-3 md:px-5 rounded-full bg-white/5 backdrop-blur-md border border-white/10 text-white text-[10px] md:text-xs tracking-[0.15em] md:tracking-[0.2em] font-bold mb-6 md:mb-8 uppercase">
            <span class="w-1.5 h-1.5 md:w-2 md:h-2 rounded-full bg-green-400 animate-pulse shadow-[0_0_10px_rgba(74,222,128,0.5)]"></span>
            Servicio VIP a Domicilio
        </div>

        <!-- Título principal con animación de letras -->
        <h1 class="hero-title text-4xl sm:text-5xl md:text-7xl lg:text-8xl font-bold text-white mb-4 md:mb-6 tracking-tight">
            <span class="block title-word title-word-1">EVOLVE</span>
            <span class="block text-transparent bg-clip-text bg-gradient-to-r from-evolve-nude via-evolve-accent to-white title-word title-word-2 mt-1 md:mt-2">LASH</span>
        </h1>

        <!-- Subtítulo con efecto de escritura -->
        <p class="hero-subtitle text-sm sm:text-base md:text-xl text-gray-200 mb-8 md:mb-10 font-light max-w-xl md:max-w-2xl mx-auto leading-relaxed px-2">
            Expertos en diseñar miradas que cautivan. Llevamos la experiencia de un estudio de lujo a la comodidad de tu hogar.
        </p>

        <!-- Botones con hover effects mejorados -->
        <div class="hero-buttons flex flex-col sm:flex-row gap-3 md:gap-5 justify-center px-2">
            <button
                onclick="window.open('https://wa.me/51999999999?text=Hola,%20quisiera%20reservar%20una%20cita.', '_blank')"
                class="btn-primary group relative overflow-hidden bg-[#C5A065] text-white px-6 md:px-10 py-3 md:py-4 rounded-full font-bold shadow-[0_0_30px_rgba(197,160,101,0.4)] hover:shadow-[0_0_50px_rgba(197,160,101,0.6)] hover:bg-[#222222] transition-all duration-300 transform hover:scale-105 active:scale-95 cursor-pointer text-sm md:text-base"
            >
                <span class="relative z-10 flex items-center justify-center gap-2">
                    <svg class="w-4 h-4 md:w-5 md:h-5" fill="currentColor" viewBox="0 0 24 24">
                        <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
                    </svg>
                    Reservar Cita Ahora
                </span>
            </button>

            <a
                href="/services"
                class="btn-secondary group relative flex items-center justify-center bg-white/5 border border-white/30 backdrop-blur-md text-white px-6 md:px-10 py-3 md:py-4 rounded-full font-bold hover:bg-white hover:text-evolve-dark transition-all duration-300 overflow-hidden text-sm md:text-base"
            >
                <span class="relative z-10 flex items-center gap-2">
                    Ver Carta de Servicios
                    <svg class="w-3 h-3 md:w-4 md:h-4 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                    </svg>
                </span>
            </a>
        </div>

        <!-- Indicador de scroll - oculto en móvil -->
        <div class="scroll-indicator hidden md:block absolute bottom-10 left-1/2 -translate-x-1/2">
            <div class="mouse-scroll">
                <div class="mouse">
                    <div class="wheel"></div>
                </div>
                <div class="arrows">
                    <span class="arrow-down"></span>
                    <span class="arrow-down"></span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    /* Animación de entrada del contenido */
    .hero-content {
        animation: fadeInUp 1s ease-out forwards;
    }

    .badge-container {
        opacity: 0;
        animation: fadeInUp 0.8s ease-out 0.2s forwards;
    }

    .title-word-1 {
        opacity: 0;
        animation: slideInRight 0.8s ease-out 0.4s forwards;
    }

    .title-word-2 {
        opacity: 0;
        animation: slideInLeft 0.8s ease-out 0.6s forwards;
    }

    .hero-subtitle {
        opacity: 0;
        animation: fadeInUp 0.8s ease-out 0.8s forwards;
    }

    .hero-buttons {
        opacity: 0;
        animation: fadeInUp 0.8s ease-out 1s forwards;
    }

    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes slideInRight {
        from {
            opacity: 0;
            transform: translateX(-50px);
        }
        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    @keyframes slideInLeft {
        from {
            opacity: 0;
            transform: translateX(50px);
        }
        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    /* Círculos flotantes */
    .floating-circle {
        position: absolute;
        border-radius: 50%;
        filter: blur(60px);
        opacity: 0.4;
    }

    .circle-1 {
        width: 400px;
        height: 400px;
        background: linear-gradient(135deg, #C5A065, #E8D5C4);
        top: -100px;
        right: -100px;
        animation: float1 15s ease-in-out infinite;
    }

    .circle-2 {
        width: 300px;
        height: 300px;
        background: linear-gradient(135deg, #E8D5C4, #C5A065);
        bottom: -50px;
        left: -100px;
        animation: float2 18s ease-in-out infinite;
    }

    .circle-3 {
        width: 200px;
        height: 200px;
        background: #C5A065;
        top: 40%;
        left: 10%;
        animation: float3 12s ease-in-out infinite;
    }

    .circle-4 {
        width: 150px;
        height: 150px;
        background: #E8D5C4;
        top: 20%;
        right: 20%;
        animation: float1 20s ease-in-out infinite reverse;
    }

    .circle-5 {
        width: 250px;
        height: 250px;
        background: linear-gradient(45deg, #C5A065, transparent);
        bottom: 30%;
        right: -50px;
        animation: float2 16s ease-in-out infinite;
    }

    @keyframes float1 {
        0%, 100% { transform: translate(0, 0) scale(1); }
        25% { transform: translate(-20px, 30px) scale(1.05); }
        50% { transform: translate(10px, -20px) scale(0.95); }
        75% { transform: translate(30px, 10px) scale(1.02); }
    }

    @keyframes float2 {
        0%, 100% { transform: translate(0, 0) rotate(0deg); }
        33% { transform: translate(30px, -30px) rotate(10deg); }
        66% { transform: translate(-20px, 20px) rotate(-5deg); }
    }

    @keyframes float3 {
        0%, 100% { transform: translate(0, 0) scale(1); opacity: 0.4; }
        50% { transform: translate(20px, -40px) scale(1.1); opacity: 0.6; }
    }

    /* Líneas animadas */
    .animated-line {
        position: absolute;
        height: 1px;
        background: linear-gradient(90deg, transparent, rgba(197, 160, 101, 0.3), transparent);
    }

    .line-1 {
        width: 300px;
        top: 30%;
        left: 5%;
        animation: shimmer 4s ease-in-out infinite;
        transform: rotate(-15deg);
    }

    .line-2 {
        width: 200px;
        top: 60%;
        right: 10%;
        animation: shimmer 5s ease-in-out infinite 1s;
        transform: rotate(20deg);
    }

    .line-3 {
        width: 250px;
        bottom: 20%;
        left: 15%;
        animation: shimmer 4.5s ease-in-out infinite 0.5s;
        transform: rotate(-10deg);
    }

    @keyframes shimmer {
        0%, 100% { opacity: 0; transform: translateX(-50px); }
        50% { opacity: 1; transform: translateX(50px); }
    }

    /* Destellos */
    .sparkle {
        position: absolute;
        width: 4px;
        height: 4px;
        background: #C5A065;
        border-radius: 50%;
        box-shadow: 0 0 10px #C5A065, 0 0 20px #C5A065;
    }

    .sparkle-1 { top: 15%; left: 20%; animation: twinkle 3s ease-in-out infinite; }
    .sparkle-2 { top: 25%; right: 25%; animation: twinkle 2.5s ease-in-out infinite 0.5s; }
    .sparkle-3 { top: 45%; left: 30%; animation: twinkle 4s ease-in-out infinite 1s; }
    .sparkle-4 { top: 65%; right: 15%; animation: twinkle 3.5s ease-in-out infinite 0.3s; }
    .sparkle-5 { bottom: 25%; left: 15%; animation: twinkle 2.8s ease-in-out infinite 0.8s; }
    .sparkle-6 { bottom: 35%; right: 30%; animation: twinkle 3.2s ease-in-out infinite 1.2s; }

    @keyframes twinkle {
        0%, 100% { opacity: 0; transform: scale(0); }
        50% { opacity: 1; transform: scale(1); }
    }

    /* Scroll indicator */
    .scroll-indicator {
        opacity: 0;
        animation: fadeInUp 1s ease-out 1.5s forwards;
    }

    .mouse {
        width: 26px;
        height: 40px;
        border: 2px solid rgba(255, 255, 255, 0.5);
        border-radius: 13px;
        display: flex;
        justify-content: center;
        padding-top: 8px;
    }

    .wheel {
        width: 4px;
        height: 8px;
        background: rgba(255, 255, 255, 0.7);
        border-radius: 2px;
        animation: scroll-wheel 2s ease-in-out infinite;
    }

    @keyframes scroll-wheel {
        0%, 100% { transform: translateY(0); opacity: 1; }
        50% { transform: translateY(8px); opacity: 0.3; }
    }

    .arrows {
        display: flex;
        flex-direction: column;
        align-items: center;
        margin-top: 5px;
    }

    .arrow-down {
        width: 10px;
        height: 10px;
        border-right: 2px solid rgba(255, 255, 255, 0.5);
        border-bottom: 2px solid rgba(255, 255, 255, 0.5);
        transform: rotate(45deg);
        margin-top: -5px;
        animation: arrow-bounce 2s ease-in-out infinite;
    }

    .arrow-down:nth-child(2) {
        animation-delay: 0.2s;
    }

    @keyframes arrow-bounce {
        0%, 100% { opacity: 0.3; transform: rotate(45deg) translateY(0); }
        50% { opacity: 1; transform: rotate(45deg) translateY(5px); }
    }

    /* Efecto brillo en título */
    .title-word-2 {
        position: relative;
        background-size: 200% auto;
        animation: slideInLeft 0.8s ease-out 0.6s forwards, shimmer-text 3s linear infinite 1.5s;
    }

    @keyframes shimmer-text {
        0% { background-position: 0% center; }
        100% { background-position: 200% center; }
    }

    /* Hover effects para botones */
    .btn-primary:hover .relative.z-10 {
        color: white;
    }

    /* Responsive */
    @media (max-width: 768px) {
        .floating-circle {
            filter: blur(40px);
        }

        .circle-1 {
            width: 200px;
            height: 200px;
        }

        .circle-2 {
            width: 150px;
            height: 150px;
        }

        .scroll-indicator {
            display: none;
        }
    }

    /* Patrón decorativo sutil */
    .lash-pattern {
        opacity: 0.03;
        background-image: radial-gradient(circle at 50% 50%, #C5A065 1px, transparent 1px);
        background-size: 40px 40px;
    }
</style>
